@default = "isEditable"
definition {

	function isEditable(locator1) {
		WaitForSPARefresh();

		if (selenium.isElementPresent(${locator1})) {
			selenium.isEditable(${locator1});
		}
		else {
			selenium.isElementPresent(${locator1});
		}
	}

}