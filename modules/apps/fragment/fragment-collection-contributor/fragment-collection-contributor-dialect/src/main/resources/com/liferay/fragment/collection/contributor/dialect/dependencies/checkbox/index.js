if (configuration.indeterminate) {
	fragmentElement.querySelector('.custom-control-input').indeterminate = true;
}

if (configuration.checked) {
	fragmentElement.querySelector('.custom-control-input').checked = true;
}
